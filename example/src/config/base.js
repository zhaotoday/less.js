// 分页大小
const PAGE_SIZE = 10

// 静态资源版本
const STATIC_VERSION = '0.7'

// URL Rewrite
const REWRITES = []

// APP KEYS
const APP_KEYS = ['keys', 'keykeys']

module.exports = {
  PAGE_SIZE,
  STATIC_VERSION,
  REWRITES,
  APP_KEYS
}
